services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: fluxo
      POSTGRES_PASSWORD: fluxo
      POSTGRES_DB: fluxo
    ports:
      - "5432:5432"
    volumes:
      - fluxo_db_data:/var/lib/postgresql/data

volumes:
  fluxo_db_data:
